---
/**
 * Landing für Besucher: einladend, Hinweis dass Event-Seite im Aufbau ist.
 * Eingeloggte Admins werden clientseitig zu /start weitergeleitet.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import StickyHeader from '../components/layout/StickyHeader.astro';
---

<BaseLayout title="kiezling - Familienaktivitäten in Karlsruhe" description="kiezling – Event-Seite im Aufbau. Anmelden für Zugang.">
  <StickyHeader />
  <script is:inline>
    (function() {
      if (typeof window === 'undefined') return;
      if (localStorage.getItem('auth_token') || sessionStorage.getItem('auth_token')) {
        window.location.replace('/start');
      }
    })();
  </script>

  <main id="main-content" role="main" class="flex-1 flex flex-col items-center justify-center px-4 py-16 sm:py-24 bg-tagestipp-bg">
    <div class="w-full max-w-lg bg-tagestipp-card shadow-tagestipp rounded-tagestipp-card p-8 sm:p-10 text-center">
      <p class="text-sm text-tagestipp-muted mb-4" aria-hidden="true">Bald für dich da</p>
      <div class="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-primary-100 text-primary-600 mb-6" aria-hidden="true">
        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
        </svg>
      </div>
      <h1 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-2">kiezling</h1>
      <p class="text-gray-600 mb-6">Familienaktivitäten in Karlsruhe</p>
      <p class="text-gray-700 mb-3">
        Unsere Event-Seite entsteht gerade – wir sammeln schon tolle Tipps für Familien in und um Karlsruhe.
      </p>
      <p class="text-gray-500 text-sm mb-8">
        Du kannst dich schon jetzt anmelden und bekommst Zugang, sobald es losgeht.
      </p>
      <a
        href="/login"
        class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-primary-500 text-white font-semibold rounded-full hover:bg-primary-600 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 transition-colors"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1" />
        </svg>
        Anmelden
      </a>
    </div>
  </main>
</BaseLayout>

---
// StickyHeader.astro - Sticky navigation with search, location, and profile
import SearchAutocomplete from '../search/SearchAutocomplete.astro';
---

<header id="sticky-header" class="sticky top-0 z-50 bg-white/95 backdrop-blur-md border-b border-gray-100 transition-all duration-300" role="banner">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <nav id="main-navigation" class="flex items-center justify-between h-16 md:h-20 gap-4" aria-label="Hauptnavigation">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-2 flex-shrink-0 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 rounded-lg" aria-label="familienlokal - Startseite">
        <div class="w-10 h-10 bg-gradient-to-br from-primary-500 to-primary-600 rounded-xl flex items-center justify-center" aria-hidden="true">
          <span class="text-white text-xl">🏠</span>
        </div>
        <span class="hidden sm:block text-xl font-bold text-gray-900">familien<span class="text-primary-500">lokal</span></span>
      </a>

      <!-- Search Bar (Desktop) -->
      <div class="hidden md:flex flex-1 max-w-2xl mx-8">
        <div class="flex w-full bg-gray-50 rounded-full border border-gray-200 hover:border-gray-300 transition-colors">
          <!-- Search Input with Autocomplete -->
          <SearchAutocomplete inputId="desktop-search" />
          
          <!-- Divider -->
          <div class="w-px bg-gray-200 my-2"></div>
          
          <!-- Location -->
          <button class="flex items-center gap-2 px-4 py-3 text-gray-600 hover:text-gray-900 transition-colors" id="location-btn">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
            </svg>
            <span class="text-sm font-medium" id="location-text">Karlsruhe</span>
            <span class="text-xs text-gray-400">10 km</span>
          </button>
          
          <!-- Search Button -->
          <button class="px-5 py-3 bg-primary-500 text-white rounded-full hover:bg-primary-600 transition-colors flex items-center gap-2" id="header-search-btn">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
            </svg>
          </button>
        </div>
      </div>

      <!-- Right Side -->
      <div class="flex items-center gap-2">
        <!-- Mobile Search Button -->
        <button 
          class="md:hidden p-2 text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-full transition-colors focus:outline-none focus:ring-2 focus:ring-primary-500" 
          id="mobile-search-btn"
          aria-label="Suche öffnen"
          aria-expanded="false"
          aria-controls="mobile-search-overlay"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
          </svg>
        </button>
        
        <!-- Merkliste -->
        <a href="/merkliste" class="hidden sm:flex items-center gap-2 px-3 py-2 text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-primary-500" aria-label="Merkliste anzeigen">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
          </svg>
          <span class="text-sm font-medium">Merkliste</span>
        </a>

        <!-- Profile / Login -->
        <a href="/login" class="flex items-center gap-2 px-4 py-2 bg-primary-500 text-white rounded-full hover:bg-primary-600 transition-colors focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
          </svg>
          <span class="hidden sm:inline text-sm font-medium">Anmelden</span>
          <span class="sm:hidden sr-only">Anmelden</span>
        </a>

        <!-- Mobile Menu Button -->
        <button 
          class="md:hidden p-2 text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-primary-500" 
          id="mobile-menu-btn"
          aria-label="Menü öffnen"
          aria-expanded="false"
          aria-controls="mobile-menu"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </nav>
  </div>

  <!-- Mobile Search Overlay -->
  <div id="mobile-search-overlay" class="hidden md:hidden absolute top-full left-0 right-0 bg-white border-b border-gray-200 p-4 shadow-lg z-50">
    <div class="flex gap-2 items-start">
      <SearchAutocomplete inputId="mobile-search" placeholder="Was wollt ihr machen?" />
      <button class="px-4 py-3 bg-primary-500 text-white rounded-xl" id="mobile-search-submit">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
        </svg>
      </button>
    </div>
    <div class="flex items-center gap-2 mt-3 text-gray-600">
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
      </svg>
      <span class="text-sm">Karlsruhe</span>
      <span class="text-xs text-gray-400">• 10 km Radius</span>
    </div>
  </div>

  <!-- Mobile Menu -->
  <nav id="mobile-menu" class="hidden md:hidden absolute top-full left-0 right-0 bg-white border-b border-gray-200 shadow-lg">
    <div class="px-4 py-4 space-y-2">
      <a href="/" class="block px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Startseite</a>
      <a href="/suche" class="block px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Suche</a>
      <a href="/plan" class="block px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Plan erstellen</a>
      <a href="/merkliste" class="block px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Merkliste</a>
      <a href="/anbieter" class="block px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Für Anbieter</a>
      <hr class="my-2" />
      <a href="/login" class="block px-4 py-3 bg-primary-500 text-white text-center rounded-lg font-medium">Anmelden</a>
    </div>
  </nav>
</header>

<script>
  // Mobile menu toggle
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const mobileSearchBtn = document.getElementById('mobile-search-btn');
  const mobileSearchOverlay = document.getElementById('mobile-search-overlay');

  mobileMenuBtn?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
    mobileSearchOverlay?.classList.add('hidden');
  });

  mobileSearchBtn?.addEventListener('click', () => {
    mobileSearchOverlay?.classList.toggle('hidden');
    mobileMenu?.classList.add('hidden');
  });

  // Header shrink on scroll
  const header = document.getElementById('sticky-header');
  
  window.addEventListener('scroll', () => {
    const currentScroll = window.pageYOffset;
    
    if (currentScroll > 100) {
      header?.classList.add('shadow-md');
    } else {
      header?.classList.remove('shadow-md');
    }
  });
</script>

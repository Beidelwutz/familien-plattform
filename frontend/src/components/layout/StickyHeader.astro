---
// StickyHeader.astro - Sticky navigation with search, location, and profile
import SearchAutocomplete from '../search/SearchAutocomplete.astro';
---

<header id="sticky-header" class="sticky top-0 z-50 bg-white/95 backdrop-blur-md border-b border-gray-100 transition-all duration-300" role="banner">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <nav id="main-navigation" class="flex items-center justify-between h-16 md:h-20 gap-4" aria-label="Hauptnavigation">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-2 flex-shrink-0 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 rounded-lg" aria-label="familienlokal - Startseite">
        <div class="w-10 h-10 bg-gradient-to-br from-primary-500 to-primary-600 rounded-xl flex items-center justify-center" aria-hidden="true">
          <span class="text-white text-xl">🏠</span>
        </div>
        <span class="hidden sm:block text-xl font-bold text-gray-900">familien<span class="text-primary-500">lokal</span></span>
      </a>

      <!-- Search Bar (Desktop) -->
      <div class="hidden md:flex flex-1 max-w-2xl mx-8">
        <div class="flex w-full bg-gray-50 rounded-full border border-gray-200 hover:border-gray-300 transition-colors">
          <!-- Search Input with Autocomplete -->
          <SearchAutocomplete inputId="desktop-search" />
          
          <!-- Divider -->
          <div class="w-px bg-gray-200 my-2"></div>
          
          <!-- Location -->
          <button class="flex items-center gap-2 px-4 py-3 text-gray-600 hover:text-gray-900 transition-colors" id="location-btn">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
            </svg>
            <span class="text-sm font-medium" id="location-text">Karlsruhe</span>
            <span class="text-xs text-gray-400">10 km</span>
          </button>
          
          <!-- Search Button -->
          <button class="px-5 py-3 bg-primary-500 text-white rounded-full hover:bg-primary-600 transition-colors flex items-center gap-2" id="header-search-btn">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
            </svg>
          </button>
        </div>
      </div>

      <!-- Right Side -->
      <div class="flex items-center gap-2">
        <!-- Mobile Search Button -->
        <button 
          class="md:hidden p-2 text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-full transition-colors focus:outline-none focus:ring-2 focus:ring-primary-500" 
          id="mobile-search-btn"
          aria-label="Suche öffnen"
          aria-expanded="false"
          aria-controls="mobile-search-overlay"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
          </svg>
        </button>
        
        <!-- Merkliste -->
        <a href="/merkliste" class="hidden sm:flex items-center gap-2 px-3 py-2 text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-primary-500" aria-label="Merkliste anzeigen">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
          </svg>
          <span class="text-sm font-medium">Merkliste</span>
        </a>

        <!-- Profile / Login - Logged out state -->
        <a href="/login" id="login-btn" class="flex items-center gap-2 px-4 py-2 bg-primary-500 text-white rounded-full hover:bg-primary-600 transition-colors focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
          </svg>
          <span class="hidden sm:inline text-sm font-medium">Anmelden</span>
          <span class="sm:hidden sr-only">Anmelden</span>
        </a>

        <!-- Profile / Login - Logged in state -->
        <div id="user-menu" class="relative hidden">
          <button 
            id="user-menu-btn"
            class="flex items-center gap-2 px-3 py-2 text-gray-700 hover:bg-gray-100 rounded-full transition-colors focus:outline-none focus:ring-2 focus:ring-primary-500"
            aria-expanded="false"
            aria-haspopup="true"
          >
            <div class="w-8 h-8 bg-primary-100 text-primary-600 rounded-full flex items-center justify-center">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
              </svg>
            </div>
            <span id="user-email" class="hidden sm:inline text-sm font-medium max-w-[120px] truncate"></span>
            <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>

          <!-- Dropdown Menu -->
          <div id="user-dropdown" class="hidden absolute right-0 mt-2 w-56 bg-white rounded-xl shadow-lg border border-gray-100 py-2 z-50">
            <div class="px-4 py-2 border-b border-gray-100">
              <p class="text-sm font-medium text-gray-900" id="dropdown-email"></p>
              <p class="text-xs text-gray-500" id="dropdown-role"></p>
            </div>
            <a href="/merkliste" class="flex items-center gap-3 px-4 py-2 text-gray-700 hover:bg-gray-50 transition-colors">
              <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
              </svg>
              Merkliste
            </a>
            <a href="/einstellungen" class="flex items-center gap-3 px-4 py-2 text-gray-700 hover:bg-gray-50 transition-colors">
              <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
              </svg>
              Einstellungen
            </a>
            <div id="admin-link" class="hidden">
              <a href="/admin" class="flex items-center gap-3 px-4 py-2 text-gray-700 hover:bg-gray-50 transition-colors">
                <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
                </svg>
                Admin
              </a>
            </div>
            <div id="provider-link" class="hidden">
              <a href="/anbieter/dashboard" class="flex items-center gap-3 px-4 py-2 text-gray-700 hover:bg-gray-50 transition-colors">
                <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                </svg>
                Anbieter-Dashboard
              </a>
            </div>
            <hr class="my-2" />
            <button id="logout-btn" class="flex items-center gap-3 px-4 py-2 text-red-600 hover:bg-red-50 transition-colors w-full text-left">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
              </svg>
              Abmelden
            </button>
          </div>
        </div>

        <!-- Mobile Menu Button -->
        <button 
          class="md:hidden p-2 text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-primary-500" 
          id="mobile-menu-btn"
          aria-label="Menü öffnen"
          aria-expanded="false"
          aria-controls="mobile-menu"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </nav>
  </div>

  <!-- Mobile Search Overlay -->
  <div id="mobile-search-overlay" class="hidden md:hidden absolute top-full left-0 right-0 bg-white border-b border-gray-200 p-4 shadow-lg z-50">
    <div class="flex gap-2 items-start">
      <SearchAutocomplete inputId="mobile-search" placeholder="Was wollt ihr machen?" />
      <button class="px-4 py-3 bg-primary-500 text-white rounded-xl" id="mobile-search-submit">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
        </svg>
      </button>
    </div>
    <div class="flex items-center gap-2 mt-3 text-gray-600">
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
      </svg>
      <span class="text-sm">Karlsruhe</span>
      <span class="text-xs text-gray-400">• 10 km Radius</span>
    </div>
  </div>

  <!-- Mobile Menu -->
  <nav id="mobile-menu" class="hidden md:hidden absolute top-full left-0 right-0 bg-white border-b border-gray-200 shadow-lg">
    <div class="px-4 py-4 space-y-2">
      <a href="/" class="block px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Startseite</a>
      <a href="/suche" class="block px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Suche</a>
      <a href="/plan" class="block px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Plan erstellen</a>
      <a href="/merkliste" class="block px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Merkliste</a>
      <a href="/anbieter" class="block px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Für Anbieter</a>
      <hr class="my-2" />
      <!-- Mobile: Logged out -->
      <a href="/login" id="mobile-login-btn" class="block px-4 py-3 bg-primary-500 text-white text-center rounded-lg font-medium">Anmelden</a>
      <!-- Mobile: Logged in -->
      <div id="mobile-user-section" class="hidden space-y-2">
        <div class="px-4 py-2 text-sm text-gray-500" id="mobile-user-email"></div>
        <a href="/einstellungen" class="block px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Einstellungen</a>
        <a href="/admin" id="mobile-admin-link" class="hidden px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Admin</a>
        <a href="/anbieter/dashboard" id="mobile-provider-link" class="hidden px-4 py-3 text-gray-700 hover:bg-gray-50 rounded-lg font-medium">Anbieter-Dashboard</a>
        <button id="mobile-logout-btn" class="block w-full px-4 py-3 text-red-600 hover:bg-red-50 rounded-lg font-medium text-left">Abmelden</button>
      </div>
    </div>
  </nav>
</header>

<script>
  // Mobile menu toggle
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const mobileSearchBtn = document.getElementById('mobile-search-btn');
  const mobileSearchOverlay = document.getElementById('mobile-search-overlay');

  mobileMenuBtn?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
    mobileSearchOverlay?.classList.add('hidden');
  });

  mobileSearchBtn?.addEventListener('click', () => {
    mobileSearchOverlay?.classList.toggle('hidden');
    mobileMenu?.classList.add('hidden');
  });

  // Header shrink on scroll
  const header = document.getElementById('sticky-header');
  
  window.addEventListener('scroll', () => {
    const currentScroll = window.pageYOffset;
    
    if (currentScroll > 100) {
      header?.classList.add('shadow-md');
    } else {
      header?.classList.remove('shadow-md');
    }
  });

  // User menu dropdown toggle
  const userMenuBtn = document.getElementById('user-menu-btn');
  const userDropdown = document.getElementById('user-dropdown');

  userMenuBtn?.addEventListener('click', (e) => {
    e.stopPropagation();
    userDropdown?.classList.toggle('hidden');
    userMenuBtn.setAttribute('aria-expanded', userDropdown?.classList.contains('hidden') ? 'false' : 'true');
  });

  // Close dropdown when clicking outside
  document.addEventListener('click', (e) => {
    const target = e.target as Node | null;
    if (!userMenuBtn?.contains(target) && !userDropdown?.contains(target)) {
      userDropdown?.classList.add('hidden');
      userMenuBtn?.setAttribute('aria-expanded', 'false');
    }
  });

  // Auth state management
  const API_BASE = import.meta.env?.PUBLIC_API_URL || window.location.origin.includes('localhost') 
    ? 'http://localhost:4000' 
    : '';

  async function checkAuthState() {
    const token = localStorage.getItem('auth_token');
    
    const loginBtn = document.getElementById('login-btn');
    const userMenu = document.getElementById('user-menu');
    const mobileLoginBtn = document.getElementById('mobile-login-btn');
    const mobileUserSection = document.getElementById('mobile-user-section');

    if (!token) {
      // Not logged in
      loginBtn?.classList.remove('hidden');
      userMenu?.classList.add('hidden');
      mobileLoginBtn?.classList.remove('hidden');
      mobileUserSection?.classList.add('hidden');
      return;
    }

    try {
      // Verify token and get user info
      const response = await fetch(`${API_BASE}/api/auth/me`, {
        headers: { 'Authorization': `Bearer ${token}` }
      });

      if (!response.ok) {
        // Token invalid - clear and show login
        localStorage.removeItem('auth_token');
        loginBtn?.classList.remove('hidden');
        userMenu?.classList.add('hidden');
        mobileLoginBtn?.classList.remove('hidden');
        mobileUserSection?.classList.add('hidden');
        return;
      }

      const data = await response.json();
      const user = data.data;

      // Show logged in state
      loginBtn?.classList.add('hidden');
      userMenu?.classList.remove('hidden');
      mobileLoginBtn?.classList.add('hidden');
      mobileUserSection?.classList.remove('hidden');

      // Update user info
      const userEmail = document.getElementById('user-email');
      const dropdownEmail = document.getElementById('dropdown-email');
      const dropdownRole = document.getElementById('dropdown-role');
      const mobileUserEmail = document.getElementById('mobile-user-email');

      if (userEmail) userEmail.textContent = user.email.split('@')[0];
      if (dropdownEmail) dropdownEmail.textContent = user.email;
      if (mobileUserEmail) mobileUserEmail.textContent = user.email;
      
      const roleLabels: Record<string, string> = {
        'admin': 'Administrator',
        'provider': 'Anbieter',
        'parent': 'Familie'
      };
      if (dropdownRole) dropdownRole.textContent = roleLabels[user.role] || user.role;

      // Show admin link for admin users
      if (user.role === 'admin') {
        document.getElementById('admin-link')?.classList.remove('hidden');
        document.getElementById('mobile-admin-link')?.classList.remove('hidden');
      }

      // Show provider link for provider users
      if (user.role === 'provider' || user.role === 'admin') {
        document.getElementById('provider-link')?.classList.remove('hidden');
        document.getElementById('mobile-provider-link')?.classList.remove('hidden');
      }

    } catch (error) {
      console.error('Auth check failed:', error);
      // On error, show login button
      loginBtn?.classList.remove('hidden');
      userMenu?.classList.add('hidden');
      mobileLoginBtn?.classList.remove('hidden');
      mobileUserSection?.classList.add('hidden');
    }
  }

  // Logout function - clears both localStorage and Supabase session
  async function handleLogout() {
    try {
      // Clear local storage
      localStorage.removeItem('auth_token');
      
      // Try to sign out from Supabase dynamically
      try {
        const { supabase } = await import('../../lib/supabase');
        await supabase.auth.signOut();
      } catch {
        // Supabase not available, continue
      }

      // Redirect to home
      window.location.href = '/';
    } catch (error) {
      console.error('Logout error:', error);
      // Still clear token and redirect on error
      localStorage.removeItem('auth_token');
      window.location.href = '/';
    }
  }

  // Attach logout handlers
  document.getElementById('logout-btn')?.addEventListener('click', handleLogout);
  document.getElementById('mobile-logout-btn')?.addEventListener('click', handleLogout);

  // Check auth state on load
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', checkAuthState);
  } else {
    checkAuthState();
  }

  // Listen for storage changes (login/logout in other tabs)
  window.addEventListener('storage', (e) => {
    if (e.key === 'auth_token') {
      checkAuthState();
    }
  });
</script>

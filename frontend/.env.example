# Backend API base URL. Used for SSR and client fetch (index, suche, event detail).
# In production (Vercel): set PUBLIC_API_URL in Project Settings → Environment Variables.
# Example: https://api.kiezling.com
PUBLIC_API_URL="http://localhost:4000"

# AI Worker URL (Python service for crawling, classification, plan generation)
# The AI Worker runs separately and cannot be hosted on Vercel.
# For local development: http://localhost:5000
# For production: Deploy to Railway/Render/own server and set URL here
PUBLIC_AI_WORKER_URL="http://localhost:5000"

# Supabase (for authentication)
# Get these from: Supabase Dashboard > Settings > API
PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
